"""
Entry point principale per Streamlit Cloud.

Questo file importa ed esegue l'applicazione principale contenuta nel
pacchetto `contabilita_francesco`. In questo modo Streamlit Cloud può
avviare l'app sia da `app.py` (root) sia da `contabilita_francesco/app.py`.
"""
from contabilita_francesco.app import *
